"use client";

/**
 * Sticky realtime banner strip. Phase A scaffold — renders nothing yet.
 *
 * Next phase wires it to `announcements` via supabase realtime per the
 * brief (Section 9). Dismissals persist to localStorage so a dismissed
 * banner stays gone for that user.
 */
export function BannerStrip() {
  return null;
}
