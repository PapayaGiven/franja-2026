"use client";

/**
 * "Tu conferencia inicia en X min" banner. Phase A scaffold — renders
 * nothing until the favorites store + supabase query are wired in
 * (Phase E in the brief).
 */
export function UpcomingFavoriteBanner() {
  return null;
}
